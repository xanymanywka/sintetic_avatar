# Synthetic Focus Group для HUMANIST

Інструмент для тестування гіпотез на 50 синтетичних персонах, створених на основі реальних даних GA4.

## 🚀 Деплой на Railway (5 хвилин)

### Варіант 1: Через GitHub (рекомендовано)

1. **Створи репозиторій на GitHub:**
   ```bash
   cd synthetic-focus-group
   git init
   git add .
   git commit -m "Initial commit"
   gh repo create humanist-focus-group --public --push
   ```

2. **Підключи до Railway:**
   - Зайди на [railway.app](https://railway.app)
   - New Project → Deploy from GitHub repo
   - Обери `humanist-focus-group`
   - Railway автоматично побачить Dockerfile

3. **Налаштуй змінні (опціонально):**
   - Settings → Variables
   - Додай `ANTHROPIC_API_KEY` якщо хочеш Claude API замість симуляції

4. **Отримай URL:**
   - Settings → Domains → Generate Domain
   - Твій сервіс буде доступний за адресою типу `humanist-focus-group.up.railway.app`

### Варіант 2: Railway CLI

```bash
# Встанови Railway CLI
npm install -g @railway/cli

# Залогінся
railway login

# Створи проект та задеплой
cd synthetic-focus-group
railway init
railway up
```

## 📁 Структура проекту

```
synthetic-focus-group/
├── backend/
│   ├── main.py          # FastAPI сервер + 50 персон
│   └── requirements.txt
├── frontend/
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── App.js       # React UI
│   │   └── index.js
│   └── package.json
├── Dockerfile           # Мультистейдж білд
├── railway.json         # Конфіг Railway
└── README.md
```

## 🔧 Локальний запуск

### Backend:
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

### Frontend:
```bash
cd frontend
npm install
npm start
```

Відкрий http://localhost:3000

## 🧠 Як це працює

1. **50 персон** створені на основі реальних даних GA4 HUMANIST:
   - Розподіл за віком, статтю, містом
   - Сегментація за STATE (сон, стрес, енергія...)
   - Lifestyle (High Achievers, Біохакери, Скептики...)

2. **Тестування гіпотез:**
   - Шкала 1-10 або вибір варіанту
   - Фільтрація за сегментами
   - Автоматичний саммарі з рекомендаціями

3. **Відповіді генеруються:**
   - Rule-based логіка (за замовчуванням)
   - Або через Claude API (якщо є ключ)

## 📊 Приклади гіпотез для тестування

- "Наскільки вам цікава підписка на CBD продукти зі знижкою 15%?"
- "Чи відвідали б ви Experience Center HUMANIST у Києві?"
- "Який формат продукту вам найбільш зручний: олія / пастилки / крем?"
- "Чи готові ви платити більше за органічну сертифікацію?"

## 🔐 Змінні середовища

| Змінна | Опис | Обов'язкова |
|--------|------|-------------|
| `ANTHROPIC_API_KEY` | API ключ для Claude | Ні (буде симуляція) |
| `PORT` | Порт сервера | Ні (за замовчуванням 8000) |

## 📝 TODO

- [ ] Інтеграція з PostgreSQL для збереження історії
- [ ] Завантаження CSV з реальними відповідями для порівняння
- [ ] Експорт результатів у PDF
- [ ] Більше персон (до 100)
- [ ] A/B тести на позиціонування

---

Зроблено для HUMANIST 🌿
